export default function logBudgedOverflows(message){
    console.log(message);
    // 📝: IMP
    // in future now the way we notify the overflow can be via email, notification and we only need to make the change here
}